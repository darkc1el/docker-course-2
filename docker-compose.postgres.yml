version: '3.8'

services:
  postgres:
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: test123
    networks:
      - my-pg-network

networks:
  my-pg-network:
    driver: bridge